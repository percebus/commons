name: Verify
on:
  push:

jobs:
  npm_test:
    runs-on: ubuntu-latest
    name: npm test
    steps:
      - uses: ../actions/checkout
      - uses: ../actions/node_setup
      - uses: ../actions/npm_install
      - name: npm test
        run: npm test

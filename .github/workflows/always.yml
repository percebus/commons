name: Verify
on:
  push:

jobs:
  npm_test:
    runs-on: ubuntu-latest
    name: npm test
    steps:
      - uses: percebus/commons/.github/actions/checkout@feat/actions
      - uses: percebus/commons/.github/actions/node_setup@feat/actions
      - uses: percebus/commons/.github/actions/npm_install@feat/actions
      - name: npm test
        run: npm test

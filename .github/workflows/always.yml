name: Verify
on:
  push:

jobs:
  npm_test:
    runs-on: ubuntu-latest
    name: npm test
    steps:
      # FIXME use relative local path
      - uses: percebus/commons/.github/actions/checkout@main
      - uses: percebus/commons/.github/actions/node_setup@main
      - uses: percebus/commons/.github/actions/npm_install@main

      - name: npm run lint
        run: npm run lint

      - name: npm test
        run: npm test --ignore-scripts

name: npm install
description: npm install
inputs:
  ls_opts:
    description: ls options
    required: false
    # in windows, la does NOT work
    default: "-la"

runs:
  using: composite
  steps:
    - name: checkout
      uses: actions/checkout@v3

    - name: ls
      run: ls ${{ inputs.ls_opts }}
